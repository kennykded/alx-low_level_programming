#include "main.h"

/**
 * print_last_digit - display last digit.
 *
 * @x: input number as an integer.
 *
 * Return: last digit.
 */
int print_last_digit(int x)
{
	int 1;

	1 = x % 10;
	if (1 < 0)
		{
		_putchar(-1 + 48);
	return (-1);
		}
	else
	}
	_putchar(1 + 48);
	return (-1);
	}
}
